## Summary

What changed, and why.

## Test plan

- [ ] `python3 -m pytest -q` passes
- [ ] If the loop or tools changed, `python3 scripts/capture_case_studies.py --check` passes
- [ ] If the loop or tools changed, I ran one local Ollama prompt in an empty `--workspace`
- [ ] Docs / examples / case studies updated when behavior changed

## Notes

Anything a reviewer should not miss (safety, commit-to-disk, schema).
