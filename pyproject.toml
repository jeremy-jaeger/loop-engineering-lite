[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "agent-loop"
version = "0.1.0"
description = "A lightweight, self-healing local AI agent harness."
requires-python = ">=3.9"

[project.scripts]
agent-loop = "main:cli_entry"